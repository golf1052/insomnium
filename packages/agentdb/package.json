{
  "private": true,
  "name": "agentdb",
  "license": "MIT",
  "version": "0.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts --cache",
    "type-check": "tsc --noEmit --project tsconfig.json",
    "test": "jest"
  }
}
